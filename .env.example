# 本地私有配置模板：复制成 .env 再填自己的值。
# .env 已被 .gitignore 忽略，不会提交；这个模板可以随仓库提交。
# 换成自己的密钥；用其他 OpenAI 兼容服务时改 OPENAI_BASE_URL 和 LLM_MODEL。

OPENAI_API_KEY=sk-替换成你自己的密钥
OPENAI_BASE_URL=https://api.deepseek.com/
LLM_MODEL=deepseek-flash
